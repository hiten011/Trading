# Dependencies for `make test` / `pytest tests`, outside Docker.
# Mirrors the versions PKScreener's own requirements.txt pins where it
# matters (numpy/pandas -- see docker/requirements-custom.txt for why).
numpy==1.26.4
pandas>=2.2.3,<2.3
pytz>=2024.2
requests>=2.32.4
python-dotenv>=1.0.1
tabulate>=0.9.0
pytest>=8.0.0

# scripts/nse_oi_buildup_scanner.py imports this at module level; its own
# tests use pytest.importorskip so a missing install only skips those tests
# rather than crashing collection, but listing it here means CI actually
# exercises them instead of always skipping.
jugaad-data>=0.35  # version actually tested against

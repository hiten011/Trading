# Extra Python packages for your own indicators.
#
# The base PKScreener image already provides:
#   pandas, numpy, scipy, scikit-learn, TA-Lib (talib), requests, pytz,
#   tabulate, python-dotenv, PKDevTools, PKNSETools, pkscreener
#
# Add anything else you need below, then rebuild:
#   docker compose build alerts
#
# Example:
# pandas-ta-classic==0.3.14b0
